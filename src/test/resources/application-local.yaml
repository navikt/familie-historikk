spring:
  datasource:
    username: postgres
    password: test
    url: jdbc:postgresql://localhost:5432/familie-historikk
    driver-class-name: org.postgresql.Driver

no.nav.security.jwt:
  issuer.azuread:
    discoveryurl: ${AZUREAD_DISCOVERY_URL}
    accepted_audience: ${AZURE_APP_CLIENT_ID}, ${AZURE_TILBAKE_APP_CLIENT_ID}

FAMILIE_TILBAKE_FRONTEND_URL: http://localhost:8000
